# FiberMind DAS Foundation (Colab pipeline)

This repo contains code + notebooks for:
1) H5 ingestion + windowing
2) WPD preprocessing
3) MAE-style foundation model training
4) Embedding evaluation + downstream sequences (xLSTM / anomaly)

Artifacts and data are stored in Google Drive, not in this repo.
